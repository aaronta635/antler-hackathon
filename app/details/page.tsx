// Phase 2 will live here: the track metadata form (title, genre, vibe, duration)
// that calls /api/reactions. Stub for now so the route exists (decision D11).
export default function DetailsPage() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center px-6 py-16">
      <p className="text-sm text-muted">Track details — coming in Phase 2.</p>
    </main>
  );
}
