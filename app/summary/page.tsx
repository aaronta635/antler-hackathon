// Phase 4 will live here: the end-of-song summary + decision layer (best moment,
// drop-off/risk, per-platform share signal). Stub for now (decisions D10, D11).
export default function SummaryPage() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center px-6 py-16">
      <p className="text-sm text-muted">Summary — coming in Phase 4.</p>
    </main>
  );
}
